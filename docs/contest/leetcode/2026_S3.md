# 2026 S3

## Biweekly Contest [#186](https://leetcode.com/contest/biweekly-contest-186/)

Q3 - Minimum Operations to Transform Binary String

這題一開始想用劃分型 dp 結果卡在狀態轉移，賽後加上圖才過。

這題可以用貪心，應該嘗試操作 1 個字母的情況，然後分類討論應該跟下一個字母一起操作還是前一個字母。

Q4 - Count Distinct Ways to Form Target from Two Strings

練習用容斥計算方案數。

* 限制: `w1` 及 `w2` 各至少選一個
    定義 $f(i, j, k)$ 為由 $w_1[0: j]$ 和 $w_2[0: k]$ 組成 target $T[0: i]$ 的方案數，包含空串。

    設 $g(s, t) 為$ s 在 t 中的方案數，透過減去 $g(w_1, T)$ 和 $g(w_2, target)$ 的方案數，可以得到至少選一個的方案數。

    $$f(w_1, w_2, tar) - g(w_1, tar) - g(w_2, tar)$$

    也就是 "選兩個構成 $T$ 的方案數 - $w_1$ 構成 $T$ 的方案數 - $w_2$ 構成 T 的方案數"


* 解決 $f(i, j, k)$:
  * 定義 $f(i, j, k)$ 為由 $w_1[0: j]$ 和 $w_2[0: k]$ 組成 target $T[0: i]$ 的方案數，包含空串。
  * 討論 $w_1[j]$, $w_2[k]$ 是否在 $T$ 中，想像成是否出現在 $T$ 的子集當中順序問題先不考慮:
    1. 都不在(都不選)
    2. 選 $w_1[j]$ 在 $T$ 中, 不在 $w_2[k]$
    3. 選 $w_2[k]$ 在 $T$ 中, 不在 $w_1[j]$
    4. $w_1[j]$ 和 $w_2[k]$，都在 $T$ 中
        當兩個都在 $T$ 時，由於 $T[i]$ 一定屬於 $w_1$ 或是 $w_2$，因此若是 $w_1[j]$ 和 $w_2[k]$ 都不匹配的話，表示 $T[i]$ 會出現在 $w_1[j]$ 跟 $w_2[k]$ 更靠前的位置，使得無法滿足題目遞增的要求。
        ```
        t  = aabbab  [b]
                   /
        w1 =   aasb  [d]
        w2 =  abxsd  [a]
        ```
  * 如果 $w_1[j]$ 不在 $T$ 中，問題變成 $T[0:i]$ 是由 $w_1[0: j - 1]$ 和 $w_2[0:k]$ 所組成的方案數為 $f(i, j - 1, k)$ 包含情況 a, c
  * 如果 $w_2[k]$ 不在 $T$ 中，問題變成 $T[0:i]$ 是由 $w_1[0: j]$ 和 $w_2[0:k - 1]$ 所組成的方案數為 $f(i, j, k - 1)$ 包含情況 a, b
  * a = $T[0:i]$ 是由 $w_1[0: j - 1]$ 和 $w_2[0:k - 1]$ 所組成的方案數 $f(i, j - 1, k - 1)$
  * 所以上面情況的方案數 $a + b + c = f(i, j - 1, k) + f(i, j, k - 1) - f(i, j - 1, k - 1)$
  * 情況 d : $T[i]$ 一定要和 $w_1[j]$ 和 $w_2[k]$ 中一個匹配
    * $T[i]$ 和 $w_1[j]$ 匹配:
        問題變成 $T[0: i - 1]$ 是由 $w_1[0: j - 1]$ 和 $w_2[0: k]$ 組成，並且 $w_2[k]$ 一定會出現在 $T[0: i - 1]$ 中:
        方案數 = $f(i - 1, j - 1, k) -$  (不符合情況 d 的方案數)
              = $f(i - 1, j - 1, k) -$ (不選 $w_2[k]$ 的方案數)
              = $f(i - 1, j - 1, k) -$ (在 $w_1[0: j - 1]$ 和 $w_2[0:k-1]$ 組成 $T[0:i-1]$ 的方案數)
              = $f(i - 1, j - 1, k) - f(i - 1, j - 1, k - 1)$


  * 邊界定義:
    * $f(j < -1) = f(k < -1) = 0$ ，-1 只是表示用完是合法的位置， $< -1$ 是越界要回傳 0
    * $f(j + 1 + k + 1 < i + 1) = 0$ : 剩下的 $w_1, w_2$無法構成 $T$
    * 最後若是 $f(i < 0) = 1$

## Weekly contest [#509](https://leetcode.cn/contest/weekly-contest-509/)

Q2 - Subsequence After One Replacement

這題砸了一個子序列自動機，wa了很多次，主要問題是沒觀察到題目的核心思想。

用前後綴分解的話，可以很容易知道若是前後綴的位置為空集合那就無法構成。

**改一個元素要想到的是前後綴分解**

Q3 - Divisible Game

這題同樣是 kadane 的變形，比賽的時候沒有意識到，只有想到枚舉左右端點，如果 n 再大一點就只能 kadane了，因為 k 的限制可以只用質因數計算。

Q4 - Palindromic Subarray Sum